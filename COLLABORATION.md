# 협업 가이드 (Collaboration Guide)

## 📋 프로젝트 구조

```
skn-2nd-3team/
├── data/
│   └── raw/
│       └── BankChurners.csv
├── notebook/
│   ├── 01_EDA.ipynb                    # 탐색적 데이터 분석 (공통)
│   ├── 02_Baseline.ipynb               # 베이스라인 모델 (공통)
│   ├── feature_engineering/            # 피처 엔지니어링
│   │   └── FE_손현우.ipynb
│   ├── ensemble/                       # 앙상블 전략
│   │   ├── Ensemble_이명준.ipynb
│   │   └── Ensemble_문지영.ipynb
│   └── cross_validation/               # 교차 검증
│       ├── CV_김준석.ipynb
│       └── CV_신병탁.ipynb
├── preprocessing.ipynb                 # 전처리 (공통)
├── README.md
├── requirements.txt
└── COLLABORATION.md                    # 이 파일
```

## 👥 담당자별 작업 영역

| 담당자 | 작업 내용 | 파일 경로 |
|--------|----------|-----------|
| 손현우 | 피처 엔지니어링 | `notebook/feature_engineering/FE_손현우.ipynb` |
| 이명준 | 앙상블 전략 | `notebook/ensemble/Ensemble_이명준.ipynb` |
| 문지영 | 앙상블 전략 | `notebook/ensemble/Ensemble_문지영.ipynb` |
| 김준석 | CV Fold 전략 | `notebook/cross_validation/CV_김준석.ipynb` |
| 신병탁 | CV Fold 전략 | `notebook/cross_validation/CV_신병탁.ipynb` |

## 🔀 Git 워크플로우

### 1. 작업 시작하기

```bash
# 1. 최신 main 브랜치 가져오기
git checkout main
git pull origin main

# 2. 새로운 feature 브랜치 생성
git checkout -b feature/담당자이름-작업내용
# 예: git checkout -b feature/손현우-feature-engineering
# 예: git checkout -b feature/이명준-ensemble
```

### 2. 브랜치 네이밍 컨벤션

```
feature/담당자이름-작업내용
```

**예시:**
- `feature/손현우-feature-engineering`
- `feature/이명준-ensemble-strategy`
- `feature/김준석-cross-validation`
- `feature/신병탁-cv-fold`
- `feature/문지영-ensemble-approach`

### 3. 작업 중 규칙

#### ✅ 해야 할 일

1. **작업 전 항상 pull**
   ```bash
   git checkout main
   git pull origin main
   git checkout feature/본인-브랜치
   git merge main  # 또는 git rebase main
   ```

2. **자신의 디렉토리에서만 작업**
   - 각자의 폴더(`notebook/feature_engineering/`, `notebook/ensemble/` 등)에서만 파일 수정/생성
   - 다른 사람의 디렉토리나 공통 파일 수정 시 사전 협의

3. **작업 단위로 커밋**
   ```bash
   git add notebook/feature_engineering/FE_손현우.ipynb
   git commit -m "feat: 피처 엔지니어링 - 상호작용 변수 추가"
   ```

4. **의미 있는 커밋 메시지 작성**
   ```
   형식: [타입] 작업내용
   
   타입:
   - feat: 새로운 기능 추가
   - fix: 버그 수정
   - refactor: 코드 리팩토링
   - docs: 문서 수정
   - style: 코드 포맷팅
   ```

#### ❌ 하지 말아야 할 일

1. **다른 사람의 파일 직접 수정 금지**
   - 다른 담당자의 노트북 파일을 직접 수정하지 말 것
   - 수정이 필요하면 이슈 생성 후 담당자에게 요청

2. **공통 파일 수정 전 협의 필수**
   - `02_Baseline.ipynb`, `preprocessing.ipynb` 등 공통 파일 수정 시 팀원들과 먼저 논의

3. **main 브랜치에 직접 push 금지**
   - 반드시 feature 브랜치를 통해 작업하고 PR 생성

4. **큰 파일 커밋 금지**
   - 데이터 파일, 모델 파일 등은 `.gitignore`에 등록
   - 필요시 `data/processed/` 등에 업로드 (gitignore 확인)

### 4. 작업 완료 후 PR 생성

```bash
# 1. 최종 커밋 확인
git status
git log

# 2. 원격 저장소에 push
git push origin feature/본인-브랜치

# 3. GitHub/GitLab에서 Pull Request 생성
# - 제목: [담당자명] 작업내용
# - 내용: 변경사항, 실험 결과, 주의사항 등 상세 설명
```

### 5. PR 머지 후 정리

```bash
# main 브랜치로 돌아가기
git checkout main
git pull origin main

# 불필요한 브랜치 삭제 (선택사항)
git branch -d feature/본인-브랜치
git push origin --delete feature/본인-브랜치
```

## 🔄 충돌 해결 가이드

### 충돌 발생 시

1. **충돌 확인**
   ```bash
   git status  # 충돌 파일 확인
   ```

2. **충돌 해결**
   - 충돌된 파일 열기
   - `<<<<<<<`, `=======`, `>>>>>>>` 마커 확인
   - 필요한 코드만 남기고 정리
   - 충돌 마커 모두 제거

3. **해결 후 커밋**
   ```bash
   git add 충돌해결한파일
   git commit -m "fix: 충돌 해결"
   ```

### 충돌 예방 팁

1. **작업 전 항상 pull**
2. **자신의 디렉토리에서만 작업**
3. **공통 파일 수정 최소화**
4. **작은 단위로 자주 커밋하고 push**

## 📝 데이터 사용 규칙

1. **원본 데이터는 수정하지 않기**
   - `data/raw/BankChurners.csv`는 절대 수정하지 않음

2. **전처리된 데이터는 별도 저장**
   - `data/processed/` 폴더에 저장 (현재 gitignore에 포함)
   - 필요시 전처리 스크립트를 `preprocessing.ipynb`에 작성

3. **데이터 로드 경로**
   ```python
   # 원본 데이터
   df = pd.read_csv('../data/raw/BankChurners.csv')
   
   # 전처리된 데이터 (있는 경우)
   df = pd.read_csv('../data/processed/preprocessed_data.csv')
   ```

## 🚨 긴급 상황 대응

### 실수로 main에 커밋했을 때

```bash
# 커밋 전이라면
git reset --soft HEAD~1

# 이미 push했다면
git revert HEAD
# 또는 팀원들과 상의 후 해결
```

### 브랜치가 꼬였을 때

```bash
# 작업 내용 백업
git stash

# main에서 다시 시작
git checkout main
git pull origin main
git checkout -b feature/새로운-브랜치

# 백업한 작업 불러오기
git stash pop
```

## 💡 협업 팁

1. **정기적인 커밋과 push**
   - 하루 작업 마무리 시 반드시 push
   - 작업 중간중간 커밋하기

2. **PR 설명 자세히 작성**
   - 변경사항
   - 실험 결과
   - 주의사항
   - 검토 시 확인할 부분

3. **코드 리뷰 적극 활용**
   - PR 생성 후 팀원들에게 리뷰 요청
   - 리뷰 피드백 반영

4. **이슈 트래커 활용**
   - 버그나 개선사항은 이슈로 등록
   - 작업 시작 전 이슈 확인

## 📞 문의사항

작업 중 문제가 발생하거나 불확실한 점이 있으면 팀 채팅방이나 이슈에 남겨주세요.

---

**마지막 업데이트:** 2025-11-21

